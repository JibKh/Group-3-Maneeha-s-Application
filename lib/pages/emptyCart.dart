import 'package:flutter/material.dart';

// JUST A DUMMY FILE IGNORE THIS.

class EmptyCart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Empty Cart'),
    );
  }
}